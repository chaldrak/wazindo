export const environment = {
  production: true,

  //production params
  apiUrl: 'https://api.wazindo.com/public/api/admin/live/',


};
    //ng build --aot --prod --build-optimizer --output-hashing all;
